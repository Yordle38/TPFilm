{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center m-5 text-white">Liste des favoris : </h1>
    
    <div class="row">
        <div class="col-md-6">
            <h2 class="text-center text-white">Films favoris :</h2>
            <ul>
                {% for favorite in favorites if favorite.idMovie is not null %}
                    <li>{{ movie.title }}</li>
                {% endfor %}
            </ul>
        </div>

        <div class="col-md-6">
            <h2 class="text-center text-white">Séries favorites :</h2>
            <ul>
                {% for favorite in favorites if favorite.idSerie is not null %}
                    {% set serie = entityManager.getSerieById(favorite.idSerie) %}
                    <li>{{ serie.name }}</li>
                {% endfor %}
            </ul>
        </div>
    </div>

{% endblock %}