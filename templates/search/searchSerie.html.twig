{% extends 'base.html.twig' %}


{% block body %}
    <h1 class="text-center m-5">Résultats de la recherche pour {{ searchTerm }}</h1>
    <div class="container mt-4">
        <div class="row">
            {% for serie in searchResults.results %}
                <div class="col-md-4">
                    <div class="card mb-4">
                        <img src="https://image.tmdb.org/t/p/original{{ serie.poster_path }}" alt="{{ serie.name }}">
                        <div class="card-title-overlay">
                            <a href="{{ path('serie_details', {'id': serie.id}) }}" style="text-decoration:none">
                                {{serie.name}}
                            </a>
                            <p>{{ serie.overview }}</p>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}
